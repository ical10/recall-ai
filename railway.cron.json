{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "RAILPACK" },
  "deploy": {
    "startCommand": "sh -c 'cd apps/api && uv run --no-dev --frozen python -m app.jobs.nightly'",
    "restartPolicyType": "NEVER"
  }
}
