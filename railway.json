{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "set -ex; echo PWD=$(pwd); which uv; uv --version; cd apps/api && uv run alembic upgrade head && cd /app && uv run uvicorn app.main:app --app-dir apps/api --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
